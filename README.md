# ToolRunner
An extendable, OpenAI compatible MCP server that can also be used as chat client.

```JSON
{
  "mcpServers": {
    "toolrunner": {
      "command": "C:\\Programs\\ToolRunner\\ToolRunner.exe",
      "args": [
        "--mcp"
      ],
      "cwd": "C:\\Programs\\ToolRunner\\",
      "env": { }
    }
  }
}
```
